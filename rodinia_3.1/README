This directory contains sources from the [Rodinia Benchmark Suite 3.1](https://github.com/yuhc/gpu-rodinia).

File `openmp/lud/omp/lud.c` is modified to let the benchmark work on multiple matrices at the same time, via argument `matrixcount` (or `m`).
